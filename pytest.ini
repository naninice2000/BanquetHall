[pytest]
testpaths = tests
markers =
    form_test(merchant, form): identifies the merchant and form covered by a test module
    test_case_id(case_id): maps a test to its formal requirement/test case ID
